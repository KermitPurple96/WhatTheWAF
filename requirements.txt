httpx>=0.27.0
dnspython>=2.4.0
cryptography>=41.0.0
requests>=2.28.0
# Optional — enhanced features (install for full functionality)
# mmh3                   # Favicon hash matching (Shodan-style origin discovery)
# tls-client             # TLS fingerprint rotation (JA3/JA4)
# scapy                  # TCP SYN option manipulation (requires root)
# stem                   # Tor control for IP rotation
# urwid                  # TUI dashboard
# h2>=4.0.0              # HTTP/2 fingerprint rotation
# numpy                  # Statistical confidence engine (WAF scanner)
